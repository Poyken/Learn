# Package

^:update cho 2 số sau
~:update số sau cùng

# Module bundler

# Môi trường:

- dev
- test/staging
- prod

# Biến môi trường

- quản lý thông qua file .env
- đọc file env process.env.APP_NAME(cần chạy lại server)
  .env là của cả 2 môi trường:
  .env.development: dev
  .env.production:prod
