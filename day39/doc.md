# Authentication: xác thực

# Authorization: phân quyền: 2 loại: Cookie-Base và Token-Base

# Cookie-Based: req->server,server check-> pass: tạo session chứa data user lưu vào server -> trả về cookie chứa id_session và not pass: throw error
