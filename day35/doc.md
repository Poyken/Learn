# Spread với obj,arr,func

# Arguments cho callback,debounce

# clone obj,arr shallow

# sử dụng spread để tối ưu 1 dòng code

# Enhance obj literal

# destruc ở trên tham số của hàm

# Named Argument func sử dụng obj làm tham số

# Class: thông qua supper đẩy giá trị lên class cha và private thuộc tính và phương thức

# trong hàm static this là class now non-static thì this là obj

# truy cập non từ static cần chuyển this thành obj và ngược lại

# Module: ES5 CommonJS : chỉ chạy trên server và ES6 có es6 module chạy được cả server và client

# cần config nếu muốn chạy es6 trên client

# import và export default và theo name khi có cả default và name thì name phải sau default
