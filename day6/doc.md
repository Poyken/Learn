# word-spacing: khoảng cách giữa các từ

- px

# letter-spacing: khoảng cách giữa các chữ cái

- px

# line-hight: độ cao của dòng

- value:tương đối hoặc tuyệt đối
- px hoặc number
  -> nên sử dụng light-height tương đối

# text-decoration: kiểu style và có thể mix

-> các thuộc tính css luôn có giá trị mặc định

# text-transform : thay đổi về cỡ chữ và kiểu

# text-indent: thụt đầu dòng mỗi đoạn văn(chỉ dòng đầu tiên)

- kể cả thêm thẻ br thì nó cx chỉ tính theo đoạn văn
- value:px và có giá trị âm

# word-wrap :break-word -> bẻ dòng quá width

# word-break:tương tự nhưng bẻ luôn đoạn văn thành 1 hàng mới(search gg)

# text-align:mặc định là left: thẻ dùng để căn chỉnh văn bản(chỉ có thể can thiệp vào thẻ inline)
